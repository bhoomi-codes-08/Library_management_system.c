# Library_management_system.c
Library Management System  A simple console-based Library Management System written in C. It uses file handling (no database) to persist book records — including name, author, ID, price, and issue status. Supports adding new books, displaying the full catalog, searching by book ID, issuing books, and returning books.
